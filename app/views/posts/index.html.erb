<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
        <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>
<div class="content__gray content__form">
  <div class="container">
    <% @posts.each do |post| %>
      <div class="posts__items">
        <div class="post-left">
          <!-- ユーザーの画像が表示されるように、以下のsrcに値を追加してください --><img class="post__img" src="<%= " /user_images/#{post.user.image_name} " %>"> </div>
       
          <p class="posts__item">
            <!-- link_toメソッドを用いて、ユーザー詳細ページへのリンクを作成してください -->
            <%= link_to(post.user.name, "/users/#{post.user.id}",{class: "post__name"}) %> <br>
              <%= link_to(post.content, "/posts/#{post.id}") %>
          </p>
    
      </div>
      <% end %>
  </div>
</div>
</body>
</html>